# Cloudflare Pages 配置（绑定你自己的域名）
# 用法：
#   1) 安装并登录： npm i -g wrangler && wrangler login
#   2) 部署：       wrangler pages deploy dist
#   3) 在 Cloudflare Pages 控制台 自定义域 里添加你的域名（需该域名托管在 Cloudflare）
#   4) Cloudflare 自动签发 TLS 证书

name = "pigai-app"
pages_build_output_dir = "dist"
